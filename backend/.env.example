# Efferra backend — environment variables template
# Copy this file to .env and fill in values for local development.
# Never commit .env to version control.

# Django
SECRET_KEY=change-me-to-a-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database — PostgreSQL (required for local development)
# Create the database first: createuser efferra && createdb -O efferra efferra
# Default assumes passwordless local connection (typical for local PostgreSQL installs).
# If the local setup requires a password, use: postgres://efferra:thepassword@localhost:5432/efferra
DATABASE_URL=postgres://efferra@localhost:5432/efferra

# Redis — used for Celery task queue (background jobs) in later phases
# Not required yet, but documented here for the agreed architecture.
# REDIS_URL=redis://localhost:6379/0

# Internationalization
LANGUAGE_CODE=en-us
TIME_ZONE=Europe/Amsterdam
